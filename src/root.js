import React, { useEffect } from 'react';
import App from './AppNavigationState.js'

// 可扩展codePush、store的服务
export default function Root(props = {}) {
    useEffect(() => {

        return () => {

        }
    })
    
    return <App {...props}/>
}
